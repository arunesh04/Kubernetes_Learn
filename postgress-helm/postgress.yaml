postgres:
    config:
        data:
            - key: POSTGRES_DB
              value: sample_db
            - key: POSTGRES_USER
              value: root
            - key: POSTGRES_PASSWORD
              value: root